# ---------------------------------------------------------------------------
# Confidential Choice / FINX documentation — MUST NOT be pushed.
# The target GitHub repo is PUBLIC and these PDFs are stamped
# "Confidential — For External Distribution". They stay on this machine only.
# ---------------------------------------------------------------------------
# Any format the vendor docs might arrive in, not just the one seen first.
*.pdf
*.docx
*.doc
*.xlsx
*.pptx
*.tar.gz
*.zip
docs/vendor/

# Secrets — Integration Guide Sec 11.2 forbids committing credentials
.env
.env.*
!.env.example
# Session caches. These hold a live Choice session id and access token, so a
# broad pattern is deliberate: an exact filename let .choice_session.json slip
# through and reach a public repo.
*session*.json
.choice_session.json
session.json
*.session
secrets/
credentials/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
dist/
build/

# Node / Next.js
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
.turbo/
npm-debug.log*
yarn-error.log*

# Data caches
data/cache/
*.parquet
*.csv
!engine/tests/fixtures/*.csv
scripmaster_cache/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# Logs
logs/
*.log
engine/state/

# Runtime forward-test state written by the engine. Positions rather than
# secrets, but per-run state has no business being versioned.
web/data/live.json
