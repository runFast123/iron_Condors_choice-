# Core
requests>=2.31.0
pandas>=2.0.0
numpy>=1.24.0
python-dateutil>=2.8.2

# Choice FinX broker SDK.
# NOTE: kkunal imports `websocket` (websocket-client) in websockets_feed.py but
# does NOT declare it in install_requires, so `import choice_api` fails on a
# clean install. We declare it explicitly here.
kkunal>=1.3.0
websocket-client>=1.7.0
websockets>=12.0

# Underlying-only fallback data (^NSEI spot, ^INDIAVIX). Never option premiums:
# Yahoo carries no Indian option chain.
yfinance>=0.2.40

# API + persistence
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.28
psycopg[binary]>=3.1.18
pydantic>=2.6.0

# Dev
pytest>=8.0.0
pytest-cov>=5.0.0
